pages:
  stage: deploy
  image: node:20
  script:
    - cd front-demo
    - npm ci
    - npm run build
    - mkdir -p ../public
    - cp -r dist/* ../public/
  artifacts:
    paths:
      - public
  only:
    - main
